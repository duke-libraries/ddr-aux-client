require 'spec_helper'

describe Ddr::Aux::Client do
  it 'has a version number' do
    expect(Ddr::Aux::Client::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
